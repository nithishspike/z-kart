import ajaxReq from 'online-shopping-client/utils/ajax-req';
import { module, test } from 'qunit';

module('Unit | Utility | ajax req');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ajaxReq();
  assert.ok(result);
});
