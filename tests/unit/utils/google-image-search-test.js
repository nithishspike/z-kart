import googleImageSearch from 'online-shopping-client/utils/google-image-search';
import { module, test } from 'qunit';

module('Unit | Utility | google image search');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = googleImageSearch();
  assert.ok(result);
});
