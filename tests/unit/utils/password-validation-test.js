import passwordValidation from 'online-shopping-client/utils/password-validation';
import { module, test } from 'qunit';

module('Unit | Utility | password validation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = passwordValidation();
  assert.ok(result);
});
