import transformData from 'online-shopping-client/utils/transform-data';
import { module, test } from 'qunit';

module('Unit | Utility | transform data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = transformData();
  assert.ok(result);
});
